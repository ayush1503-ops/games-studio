node_modules/
dist/
.next/
server/dist/
.vercel/

# Environment files: never commit real secrets
.env
.env.local
.env.production
server/.env
server/.env.local

# Local Postgres data directory (dev only, lives outside the repo by default)
server/.pgdata/
.pgdata/

# User uploads (content lives in the database + object storage in production)
server/uploads/

# Logs and OS noise
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Test/report output
coverage/
*.tsbuildinfo
.vercel
